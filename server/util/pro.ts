import {InternalUserAccount} from '../schemas/UserAccount.schema';

export async function handleStartPro(user: InternalUserAccount) {
	// Pro baslangic islemleri
}

export async function handleCancelPro(user: InternalUserAccount) {
	// Pro iptal islemleri
}
